{
  "name": "gaia-boardgame-arena",
  "version": "1.0.0",
  "private": true,
  "description": "가이아 · 에너지 변형 체스 · 에너지 변형 마피아 — Supabase Realtime + Vercel 정적 웹 보드게임",
  "scripts": {
    "generate-config": "node scripts/generate-config.mjs",
    "predev": "node scripts/generate-config.mjs",
    "dev": "npx --yes serve public -l 3000",
    "prestart": "node scripts/generate-config.mjs",
    "start": "npx --yes serve public -l 3000",
    "build": "node scripts/generate-config.mjs"
  }
}
